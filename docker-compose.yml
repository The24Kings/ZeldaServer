name: lurk

services:
  zelda-server:
    build: .
    ports:
      - "${PORT:-8080}:${PORT:-8080}"
    environment:
      - PORT=${PORT:-8080}
      - VERBOSITY=${VERBOSITY:--vv}
    volumes:
      - ./logs:/app/logs
    restart: unless-stopped
